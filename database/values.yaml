replicaCount: 1

image:
  repository: mysql
  tag: "8.0"

service:
  type: ClusterIP
  port: 3306

containerPort: 3306

env:
  MYSQL_DATABASE: appdb
  MYSQL_USER: appuser
  MYSQL_PASSWORD: secret
  MYSQL_ROOT_PASSWORD: rootpassword

persistence:
  enabled: true
  size: 5Gi
